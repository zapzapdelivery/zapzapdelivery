export const metadata = {
  title: {
    absolute: 'ZapZap Delivery - Cardapio',
  },
};

export default function CardapioLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
